{
  "name": "food_restuarant",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "concurrently": "^6.4.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.16",
    "monk": "^7.3.4",
    "multer": "^1.4.2",
    "nodemon": "^2.0.15",
    "sharp": "^0.25.2",
    "validator": "^13.0.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node server.js",
    "client": "cd frontend && npm start",
    "client_build": "cd frontend && npm run build && serve -s build",
    "start": "concurrently \"npm run dev\" \"npm run client\" ",
    "setup": "npm install && cd frontend && npm install",
    "build": "concurrently \"npm run dev\" \"npm run client_build\" "
  },
  "author": "",
  "license": "ISC"
}
